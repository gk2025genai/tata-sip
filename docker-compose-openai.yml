version: '3.8'

services:
  websocket-server-openai:
    build: .
    ports:
      - "8765:8765"
    container_name: tata-sip-websocket-openai
    restart: unless-stopped
    environment:
      - PYTHONUNBUFFERED=1
    command: ["python", "server_openai.py"] 